{
    "routes": [
      { "src": "/api/schedule", "dest": "/api/main.py" }
    ],
    "buildCommand": "cd frontend && npm run build && cd ..",
    "installCommand": "cd frontend && npm install && cd ..",
    "outputDirectory": "frontend/dist"
  }